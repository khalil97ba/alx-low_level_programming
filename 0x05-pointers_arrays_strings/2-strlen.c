#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - checks the length of a string
 * @s:input
 * Return: Always 0
*/

int _strlen(char *s)
{
	int length;

	length = strlen(s)
	return (length);
}
